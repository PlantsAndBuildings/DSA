#ifndef MERGE_SORT_TPP
#define MERGE_SORT_TPP

template <typename T> void sorting::MergeSorter_t<T>::sort(vector<T> &list) {
  std::cout << "Sorting a vector<T> using merge sort!" << std::endl;
}

#endif
